{
  "name": "jasmine",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "watch": "bun --watch src/main.ts",
    "compile": "bun src/main.ts",
    "binaryen": "bun compile __.jas --backend binaryen && wasmtime build/a.wasm",
    "wat": "bun compile __.jas --backend wat && wasmtime build/a.wasm"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "binaryen": "^123.0.0"
  }
}
