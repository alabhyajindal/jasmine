{
  "name": "jasmine",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "watch": "bun --watch src/main.ts source.jas",
    "compile": "bun src/main.ts source.jas",
    "build": "bun run compile && wasm-merge build/main.wat main lib/utils.wat utils -o build/a.wat --enable-multimemory --emit-text && wasmtime build/a.wat"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "binaryen": "^123.0.0"
  }
}
